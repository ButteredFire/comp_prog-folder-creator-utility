#include <bits/stdc++.h>

#define ll long long
#define all(v) (v).begin(), (v).end()
#define allr(v) (v).rbegin(), (v).rend()
// getIndex returns the size of the vector if element is not found
#define getIndex(v, elem) find((v).begin(), (v).end(), (elem)) - (v).begin()

#define getI(a) cin >> a
#define getII(a, b) cin >> a >> b
#define getIII(a, b, c) cin >> a >> b >> c
#define fillArray(arr, sz) for(decltype(sz) i=0, n=(sz); i<n; i++) cin >> (arr)[i]

#define nl '\n'
#define printnl cout << '\n'
#define printspace(a) cout << (a) << " "
#define print(a) cout << (a) << '\n'

#define char2int(c) (c - '0')

using namespace std;

int main() {
	ios::sync_with_stdio(0); cin.tie(nullptr); cout.tie(nullptr);
	//freopen("input.inp", "r", stdin);
	//freopen("output.out", "w", stdout);


    //fclose(stdin);
    //fclose(stdout);
	return 0;
	// REMEMBER TO UN-COMMENT SYNC_WITH_STDIO AND REMOVE ALL PRINTF LINES!
}